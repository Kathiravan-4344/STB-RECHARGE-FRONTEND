{
  "name": "stb-recharge-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix backend start & npm --prefix frontend run dev",
    "server": "npm --prefix backend start",
    "client": "npm --prefix frontend run dev",
    "build": "npm --prefix backend install && npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}
